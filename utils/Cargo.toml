[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[lib]
name = "utils"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12.23", features = ["json", "rustls-tls"] }
